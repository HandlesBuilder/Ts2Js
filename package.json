{
  "scripts": {
    "clean": "rimraf dist/",
    "cpf": "cp -r src/ dist/",
    "tsc": "tsc",
    "cleants": "rimraf `find ./dist -name '*.ts' -type f -print`",
    "cleantsx": "rimraf `find ./dist -name '*.tsx' -type f -print`"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  }
}
