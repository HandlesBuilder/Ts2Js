{
  "scripts": {
    "clean": "rimraf dist/",
    "copy-files": "copyfiles -u 1 'src/**/**.less' dist/",
    "build": "yarn clean && tsc && yarn copy-files"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^3.0.2"
  }
}
